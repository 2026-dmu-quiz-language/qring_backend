# 인터랙티브 스토리 — 앞으로 검토할 사항

작성일: 2026-08-20 · 대상: `/api/v1/story/**` (인터랙티브 스토리 기능)
관련 문서: [INTERACTIVE_STORY_PROMPTS.md](INTERACTIVE_STORY_PROMPTS.md)

전수 검수한 결과 중, **아직 처리하지 않고 남겨둔 항목**을 정리한 문서입니다. 해결된 항목은 이 문서에서 제거합니다.

우선순위는 다음 기준입니다.

- **높음** — 사용자에게 직접 피해가 가거나, 서버를 늘리는 순간 장애가 되는 것
- **보통** — 트래픽이 늘면 문제가 되는 것
- **낮음** — 설계 취향 또는 정리 차원

---

## 1. 세션이 서버 메모리에만 존재함 · 우선순위 높음 (다중 인스턴스 전환 시)

**현재 상태.** 대화 세션은 `InteractiveStoryService`의 `ConcurrentHashMap`에 저장됩니다. DB에는 아무것도 남지 않습니다. 3시간 TTL 정리는 추가했지만 저장 위치는 그대로 메모리입니다.

**왜 문제인가.** 두 가지 상황에서 사용자가 피해를 봅니다.

*서버 재시작 시 진행 중이던 세션이 전부 소실됩니다.* 배포하거나 서버가 재기동되면 맵이 비워집니다. 사용자는 이미 30포인트를 지불했는데 다음 메시지에서 "존재하지 않거나 만료된 스토리 세션입니다"를 받고, 포인트는 환불되지 않습니다. 대화 중 배포를 하면 그 시점의 사용자 전원이 겪습니다.

*서버를 2대 이상으로 늘리면 기능 자체가 동작하지 않습니다.* A 서버에서 `/story/start`를 처리해 세션이 A의 메모리에 생겼는데 다음 `/story/chat`이 B 서버로 가면 세션을 찾지 못합니다. 요청 분배가 매번 달라서 증상이 간헐적으로 나타나 원인 추적도 어렵습니다.

**현 시점 판단.** 단일 인스턴스로 운영 중이라면 지금 구조로도 정상 동작합니다. 급하지 않습니다. 다만 **스케일 아웃을 검토하는 순간 반드시 선행되어야 하는 작업**입니다.

**해결 방향.**

- 근본 해결 — `StorySession`을 Redis에 JSON으로 저장하거나 DB 테이블로 이전. `StorySession`은 이미 순수 자바 객체라 직렬화 부담이 적습니다.
- 완화책 (단일 서버 유지 시) — 세션 시작이 아니라 **첫 퀴즈 출제 시점에 포인트를 차감**하거나, 세션 조회 실패 시 환불 경로를 추가. 작업량 대비 효과가 좋습니다.

---

## 2. `@Transactional` 안에서 OpenAI를 호출함 · 우선순위 보통

**현재 상태.** `startStorySession`이 `@Transactional`이고, 그 안에서 포인트를 차감한 뒤 OpenAI 오프닝 생성을 호출합니다.

**의도된 이점.** OpenAI 호출이 실패하면 트랜잭션이 롤백되어 차감된 포인트가 자동 복구됩니다. 이건 현재 구조 덕분에 올바르게 동작합니다.

**왜 문제인가.** 수 초가 걸리는 외부 API 호출 동안 DB 커넥션을 계속 점유합니다. 동시 사용자가 늘면 커넥션 풀이 먼저 고갈되어, 스토리와 무관한 다른 API까지 함께 느려지거나 실패합니다. 응답 타임아웃을 60초로 걸어두었으므로 최악의 경우 커넥션 하나가 60초간 묶입니다.

**해결 방향.** 트랜잭션을 쪼개고 보상 처리를 명시적으로 하는 구조로 전환합니다. 즉 짧은 트랜잭션에서 포인트를 차감해 커밋하고, 트랜잭션 밖에서 OpenAI를 호출한 뒤, 실패 시 별도 트랜잭션으로 포인트를 되돌립니다. 다만 이 방식은 보상 실패 케이스를 따로 다뤄야 해서 지금보다 복잡해집니다. 커넥션 풀에 실제 압박이 관측되기 전까지는 현재 구조가 합리적인 선택입니다.

---

## 3. 퀴즈 정답이 클라이언트에 그대로 내려감 · 유지하기로 결정

`/story/chat` 응답의 `quiz` 객체에 `correct_answer`와 `acceptable_answers`가 포함되어 전달됩니다. 클라이언트가 즉시 정답을 표시하거나 자체 채점을 할 수 있어 **의도적으로 유지하기로 했습니다.**

기록 차원에서 남겨두면, 사용자가 개발자 도구나 프록시로 응답을 들여다보면 제출 전에 정답을 알 수 있습니다. 점수나 랭킹처럼 경쟁 요소가 붙는다면 그때 다시 검토할 여지가 있습니다. 서버 측 채점은 이미 적용되어 있으므로, 그 시점에는 응답에서 정답 필드를 제거하기만 하면 됩니다.

---

## 4. 대화 히스토리 길이 제한의 부작용 · 우선순위 낮음

**현재 상태.** 토큰 비용이 무한정 증가하지 않도록 `chatHistory`를 최근 40개 메시지로 제한했습니다.

**검토할 점.** 한 세션은 퀴즈 5개 기준 대략 15~20턴(30~40개 메시지)이라 정상 흐름에서는 잘리지 않습니다. 다만 사용자가 대화를 길게 끌면 초반 맥락이 잘려나가, 프롬프트의 "이전에 나온 세부사항을 모두 기억하라"는 지시와 충돌할 수 있습니다.

**해결 방향.** 문제가 관측되면 단순 절삭 대신, 초반 몇 턴은 보존하고 중간을 요약해 넣는 방식을 고려할 수 있습니다. 우선은 현재 설정으로 두고 실제 사용 패턴을 지켜보면 충분합니다.

---

## 5. 동일 세션 동시 요청 · 우선순위 낮음

`StorySession`은 가변 객체이고 `chatHistory`는 `ArrayList`입니다. 같은 세션으로 두 요청이 동시에 들어오면 상태가 꼬일 수 있습니다.

정상적인 사용자는 AI 응답을 기다렸다가 다음 메시지를 보내므로 실제로 발생하기 어렵습니다. 다만 사용자가 전송 버튼을 연타하거나 네트워크 재시도가 겹치면 가능합니다. 필요해지면 세션 단위 잠금을 걸면 됩니다.



## 6. 스토리 DB 저장 · 방향 결정됨 — 아직 미구현

팀 논의 결과 **인게임 재화로 추가 결제한 사용자의 스토리만 저장**하는 것으로 결정되었습니다. **아직 구현하지 않았습니다** — 이 항목은 결정 사항의 기록이며, 구현 착수 전에 아래를 설계해야 합니다.

### 구현 시 설계할 것

- **저장 범위** — "스토리 저장"이 무엇을 포함하는지: 대화 전문(`chatHistory`)만인지, 출제된 퀴즈와 채점 결과까지인지, 세션 설정(캐릭터·상황·어조·언어)까지인지.
- **결제 시점** — 세션 시작 시 저장 옵션을 미리 결제하는지, 세션이 끝난 뒤 "저장하시겠습니까?"로 결제하는지. 후자라면 세션이 서버 메모리에만 있으므로(1번 항목) **결제 전에 서버가 재시작되면 저장할 데이터 자체가 사라진다**는 점을 함께 고려해야 합니다.
- **테이블 설계** — 기존 `quiz_content` / `wrong_answer` 체계는 DB에 미리 존재하는 문제를 참조하는 구조라, 실시간 생성되는 스토리 퀴즈와는 맞지 않습니다(이전 검토에서 확인: 모든 조회 쿼리가 `QuizContent` 와 JOIN 하므로 참조 없는 행은 어느 화면에도 안 나옴). 스토리 전용 테이블(예: `story_archive`)에 값을 그대로 저장하는 편이 자연스럽고, 봇 컴피티션이 `competition_wrong_answer` 로 테이블을 분리한 선례도 있습니다.
- **조회 API** — 저장된 스토리 목록·상세(다시 읽기) 엔드포인트가 새로 필요합니다.
- **재화 차감 로직** — 세션 시작 30포인트와 별도의 결제 흐름이므로, 차감·검증·실패 시 복구를 `startStorySession` 의 포인트 처리와 같은 수준으로 갖춰야 합니다.

채점 신뢰성은 서버 채점으로 이미 확보되어 있으므로, 저장될 채점 결과의 정확성은 문제되지 않습니다.

---

## 7. 기본 학습 페이지와 퀴즈 양식이 호환되지 않음 · 우선순위 높음

기본 학습 페이지(`POST /chat` → `POST /questionResult`)와 인터랙티브 스토리(`POST /api/v1/story/chat`)가 내려주는 퀴즈 JSON을 비교한 결과, **두 API의 양식이 서로 호환되지 않습니다.** 봇 컴피티션은 비교 대상에서 제외했습니다.

같은 "퀴즈"를 다루는 두 엔드포인트인데 필드 이름 규칙, 값 어휘, 식별자 유무, 채점 흐름이 모두 다릅니다. 클라이언트 작업이 시작되기 전에 양식을 맞춰두는 편이 비용이 적습니다.

### 7-1. `quiz_type` 값 어휘가 서로 다름 

DB에 실제로 들어있는 값(`sql/` 덤프 기준)과 인터랙티브 스토리가 출제하는 값이 어긋납니다.

| quiz_type | 기본 학습 (DB 실제 데이터) | 인터랙티브 스토리 |
|---|---|---|
| `subjective` | 285건 | 출제함 |
| `multiple_choice` | 150건 | 출제함 |
| `fill_in_blank` | 15건 | **출제 안 함** |
| `word_arrange` | **0건 — 기존에 없던 유형** | 출제함 |

`word_arrange` 는 기존 데이터에 한 건도 없는 새 유형입니다. 게다가 이 유형만 `options` 대신 `tiles` 배열을 쓰기 때문에, 두 API를 하나의 퀴즈 처리 로직으로 다루려면 별도 분기가 반드시 필요합니다. 스토리 프롬프트가 "한 세션에 최소 1~2개는 `word_arrange` 또는 `subjective`"를 지시하므로 드물게 나오는 것도 아닙니다.

반대로 `fill_in_blank` 은 기본 학습에만 있습니다. 스토리 프롬프트의 Format D(빈칸 채우기)는 `quiz_type` 을 `multiple_choice` 로 출제하도록 되어 있어, 같은 형태의 문제가 서로 다른 유형명으로 나갑니다.

**정할 것:** `word_arrange` 를 유지할지 여부. 빼려면 프롬프트에서 해당 형식과 "최소 1~2개" 지시를 제거하면 되고, 유지하려면 `tiles` 를 포함한 유형 목록을 두 API가 공유하는 것으로 확정해야 합니다.

### 7-2. 필드 이름 규칙이 반대

| | 기본 학습 (`ChatResponseDto`) | 인터랙티브 스토리 (`StoryChatResponse`) |
|---|---|---|
| 규칙 | camelCase | snake_case (`@JsonProperty` 로 지정) |
| 예시 | `quizId`, `quizType`, `correctAnswer` | `quiz_type`, `correct_answer`, `ai_message`, `is_quiz` |

같은 의미의 필드가 다른 이름으로 나갑니다. 스토리 응답을 camelCase로 통일하면 클라이언트가 하나의 퀴즈 모델을 공유할 수 있습니다.

### 7-3. `options` 의 타입이 다름

| | 기본 학습 | 인터랙티브 스토리 |
|---|---|---|
| 타입 | `String` — DB JSON 컬럼 원문을 그대로 전달 | 실제 JSON 배열 |

`ChatResponseDto.QuizDto.options` 는 `String` 이라 받는 쪽이 한 번 더 파싱해야 합니다. 스토리는 배열을 그대로 내려줍니다. 같은 필드명인데 받는 쪽 처리가 달라지므로, 어느 한쪽으로 맞추는 게 좋습니다. 배열 쪽이 자연스럽습니다.

### 7-4. 퀴즈 식별자가 없음

기본 학습은 `quizId` / `quizContentId` 라는 DB PK를 기준으로 채점 결과 저장, 오답 저장, 다시 풀기가 모두 동작합니다. 인터랙티브 스토리 퀴즈는 실시간 생성이라 **ID가 없습니다.** `quiz_number` 는 세션 안에서의 1~5 순번일 뿐 전역 식별자가 아닙니다. 7번 항목(스토리 저장)을 구현할 때 이 점을 함께 설계해야 합니다.

### 7-5. 채점 흐름이 정반대

| | 기본 학습 | 인터랙티브 스토리 |
|---|---|---|
| 채점 주체 | 클라이언트 | 서버 (모델은 반응만) |
| 결과 이동 방향 | 클라이언트 → 서버 (`correct` 불리언을 받음) | 서버 → 클라이언트 (`answer_result` 를 내려줌) |
| 전달 방식 | 배치 — 다 풀고 한 번에 | 스트리밍 — 턴마다 1개 |
| 결과 저장 API | `POST /questionResult` | **없음** |

`/questionResult` 는 클라이언트가 채점한 결과를 받는 구조입니다. 스토리는 서버가 채점 결과를 내려주는 구조라 방향이 반대이고, 퀴즈 ID도 없어서(8-4) 이 엔드포인트를 그대로 재사용할 수 없습니다.

### 권장 순서

1. `word_arrange` 유지 여부 결정 (8-1)
2. 스토리 응답의 필드 이름과 `options` 타입을 기본 학습 쪽과 통일 (8-2, 8-3)
3. 그다음 7번 항목(스토리 저장)과 결과 저장 엔드포인트 설계

---

## 8. 토큰 비용 실측 결과 · 참고

`gpt-4o-mini` 기준으로 세션 하나를 처음부터 끝까지 실제 호출해 측정했습니다.

| 항목 | 값 |
|---|---|
| 호출 수 | 31회 (오프닝 1 + 턴 30) |
| 입력 토큰 | 91,396 |
| 출력 토큰 | 5,220 |
| **비용** | **약 $0.0168 (원화 약 24원)** |

단가는 입력 $0.15 / 1M, 출력 $0.60 / 1M 입니다. 환율은 1,400원 기준입니다.

**턴당 비용은 약 $0.0005(0.7원)** 이고, 퀴즈 5개를 다 채우는 지점(12번째 호출)까지만 보면 **약 $0.0058(8원)** 입니다. 현재 세션 시작 비용이 30포인트이므로 포인트 대비 API 비용은 여유가 있습니다.

입력 토큰이 전체 비용의 81%를 차지합니다. 매 턴 시스템 프롬프트 전체와 대화 히스토리를 다시 보내기 때문입니다. 호출별 입력 토큰은 567 → 3,300 근처에서 안정되는데, 4번 항목의 히스토리 40개 제한이 실제로 상한을 잡아주고 있음을 확인했습니다. 더 줄이려면 시스템 프롬프트를 압축하거나 OpenAI 프롬프트 캐싱(캐시된 입력은 절반가)을 활용하는 방향이 있습니다.

> 측정 당시에는 세션 강제 종료가 없어 30턴까지 이어졌습니다. 현재는 퀴즈 5개 채점이 끝나면 서버가 세션을 종료하므로, 실제 세션 비용은 위 표보다 "12번째 호출까지" 값(약 8원)에 가깝습니다.

---

## 참고 — 검수 범위와 한계

- 검증한 것: 컴파일, 단위 테스트 18개(프롬프트 조립·서버 채점·재시도·강제 종료 로직), 실제 OpenAI API 호출 다수(첫 인사말 답장, 퀴즈 정답/오답 반응, 서버 오답 판정 존중, 유형 지정 준수, 말투-관계 분리, 오프닝 생성, 전체 세션 비용 측정 — 모두 의도대로 동작 확인, 검증에 쓴 테스트는 일회성이라 이후 삭제)
- 검증하지 못한 것: 앱을 실제로 기동한 end-to-end 확인. DB가 필요해 수행하지 못했습니다. 세션 소유권 검증, TTL 정리, 실패 시 롤백, 타임아웃 설정은 코드 검토로만 확인했으며 실제 요청으로 시험하지 않았습니다.
