# 인터랙티브 스토리 — 앞으로 검토할 사항

작성일: 2026-08-20 · 대상: `/api/v1/story/**` (인터랙티브 스토리 기능)
관련 문서: [INTERACTIVE_STORY_PROMPTS.md](INTERACTIVE_STORY_PROMPTS.md)

전수 검수한 결과 중, **아직 처리하지 않고 남겨둔 항목**을 정리한 문서입니다. 이미 고친 내용은 이 문서에 포함하지 않았습니다.

우선순위는 다음 기준입니다.

- **높음** — 사용자에게 직접 피해가 가거나, 서버를 늘리는 순간 장애가 되는 것
- **보통** — 트래픽이 늘면 문제가 되는 것
- **낮음** — 설계 취향 또는 정리 차원

---

## 1. 퀴즈 채점이 모델 판단에 의존함 · 우선순위 높음

**현재 상태.** 서버는 퀴즈의 정답을 프롬프트에 넣어주고, 모델이 돌려준 `answer_result`(`correct`/`incorrect`/`none`)를 그대로 신뢰합니다. 서버가 직접 사용자 답안과 정답을 비교하지는 않습니다.

**왜 문제인가.** 채점 결과가 결정론적이지 않습니다. `gpt-4o-mini`가 오타나 유사 표현, 대소문자 차이가 있는 답안을 관대하게 정답 처리할 여지가 남아 있습니다. 실측에서는 명확한 정답/오답을 모두 정확히 판정했지만, 애매한 입력에 대한 보장은 없습니다.

학습 앱에서 채점은 신뢰성이 핵심인 영역이라, 확률적으로 맞는 것으로는 부족합니다.

**해결 방향.** `pendingQuiz`가 이미 서버 메모리에 정답과 함께 있으므로, 서버가 직접 비교해 결과를 확정하고 모델에는 "이미 오답으로 판정되었으니 그렇게 반응하라"고 통보하는 방식으로 바꿀 수 있습니다.

- `multiple_choice`, `word_arrange` — 정답 문자열이 명확하므로 서버에서 완전히 확정 가능
- `subjective` — `acceptable_answers` 목록과 정규화 비교 후, 목록에 없으면 모델 판단에 위임하는 하이브리드

작업량은 크지 않습니다. `InteractiveStoryService.processChatTurn`에서 `answer_result`를 덮어쓰고, 프롬프트의 채점 지시문을 "판정하라"에서 "이 판정에 맞춰 반응하라"로 바꾸면 됩니다.

---

## 2. 세션이 서버 메모리에만 존재함 · 우선순위 높음 (다중 인스턴스 전환 시)

**현재 상태.** 대화 세션은 `InteractiveStoryService`의 `ConcurrentHashMap`에 저장됩니다. DB에는 아무것도 남지 않습니다. 3시간 TTL 정리는 추가했지만 저장 위치는 그대로 메모리입니다.

**왜 문제인가.** 두 가지 상황에서 사용자가 피해를 봅니다.

*서버 재시작 시 진행 중이던 세션이 전부 소실됩니다.* 배포하거나 서버가 재기동되면 맵이 비워집니다. 사용자는 이미 30포인트를 지불했는데 다음 메시지에서 "존재하지 않거나 만료된 스토리 세션입니다"를 받고, 포인트는 환불되지 않습니다. 대화 중 배포를 하면 그 시점의 사용자 전원이 겪습니다.

*서버를 2대 이상으로 늘리면 기능 자체가 동작하지 않습니다.* A 서버에서 `/story/start`를 처리해 세션이 A의 메모리에 생겼는데 다음 `/story/chat`이 B 서버로 가면 세션을 찾지 못합니다. 요청 분배가 매번 달라서 증상이 간헐적으로 나타나 원인 추적도 어렵습니다.

**현 시점 판단.** 단일 인스턴스로 운영 중이라면 지금 구조로도 정상 동작합니다. 급하지 않습니다. 다만 **스케일 아웃을 검토하는 순간 반드시 선행되어야 하는 작업**입니다.

**해결 방향.**

- 근본 해결 — `StorySession`을 Redis에 JSON으로 저장하거나 DB 테이블로 이전. `StorySession`은 이미 순수 자바 객체라 직렬화 부담이 적습니다.
- 완화책 (단일 서버 유지 시) — 세션 시작이 아니라 **첫 퀴즈 출제 시점에 포인트를 차감**하거나, 세션 조회 실패 시 환불 경로를 추가. 작업량 대비 효과가 좋습니다.

---

## 3. `@Transactional` 안에서 OpenAI를 호출함 · 우선순위 보통

**현재 상태.** `startStorySession`이 `@Transactional`이고, 그 안에서 포인트를 차감한 뒤 OpenAI 오프닝 생성을 호출합니다.

**의도된 이점.** OpenAI 호출이 실패하면 트랜잭션이 롤백되어 차감된 포인트가 자동 복구됩니다. 이건 현재 구조 덕분에 올바르게 동작합니다.

**왜 문제인가.** 수 초가 걸리는 외부 API 호출 동안 DB 커넥션을 계속 점유합니다. 동시 사용자가 늘면 커넥션 풀이 먼저 고갈되어, 스토리와 무관한 다른 API까지 함께 느려지거나 실패합니다. 응답 타임아웃을 60초로 걸어두었으므로 최악의 경우 커넥션 하나가 60초간 묶입니다.

**해결 방향.** 트랜잭션을 쪼개고 보상 처리를 명시적으로 하는 구조로 전환합니다. 즉 짧은 트랜잭션에서 포인트를 차감해 커밋하고, 트랜잭션 밖에서 OpenAI를 호출한 뒤, 실패 시 별도 트랜잭션으로 포인트를 되돌립니다. 다만 이 방식은 보상 실패 케이스를 따로 다뤄야 해서 지금보다 복잡해집니다. 커넥션 풀에 실제 압박이 관측되기 전까지는 현재 구조가 합리적인 선택입니다.

---

## 4. 퀴즈 정답이 클라이언트에 그대로 내려감 · 유지하기로 결정

`/story/chat` 응답의 `quiz` 객체에 `correct_answer`와 `acceptable_answers`가 포함되어 전달됩니다. 클라이언트가 즉시 정답을 표시하거나 자체 채점을 할 수 있어 **의도적으로 유지하기로 했습니다.**

기록 차원에서 남겨두면, 사용자가 개발자 도구나 프록시로 응답을 들여다보면 제출 전에 정답을 알 수 있습니다. 점수나 랭킹처럼 경쟁 요소가 붙는다면 그때 다시 검토할 여지가 있습니다. 그 시점에는 1번 항목(서버 채점)을 먼저 적용한 뒤 응답에서 정답 필드를 제거하는 순서가 자연스럽습니다.

---

## 5. 대화 히스토리 길이 제한의 부작용 · 우선순위 낮음

**현재 상태.** 토큰 비용이 무한정 증가하지 않도록 `chatHistory`를 최근 40개 메시지로 제한했습니다.

**검토할 점.** 한 세션은 퀴즈 5개 기준 대략 15~20턴(30~40개 메시지)이라 정상 흐름에서는 잘리지 않습니다. 다만 사용자가 대화를 길게 끌면 초반 맥락이 잘려나가, 프롬프트의 "이전에 나온 세부사항을 모두 기억하라"는 지시와 충돌할 수 있습니다.

**해결 방향.** 문제가 관측되면 단순 절삭 대신, 초반 몇 턴은 보존하고 중간을 요약해 넣는 방식을 고려할 수 있습니다. 우선은 현재 설정으로 두고 실제 사용 패턴을 지켜보면 충분합니다.

---

## 6. 동일 세션 동시 요청 · 우선순위 낮음

`StorySession`은 가변 객체이고 `chatHistory`는 `ArrayList`입니다. 같은 세션으로 두 요청이 동시에 들어오면 상태가 꼬일 수 있습니다.

정상적인 사용자는 AI 응답을 기다렸다가 다음 메시지를 보내므로 실제로 발생하기 어렵습니다. 다만 사용자가 전송 버튼을 연타하거나 네트워크 재시도가 겹치면 가능합니다. 필요해지면 세션 단위 잠금을 걸면 됩니다.

---

## 7. 프롬프트 수정 시 회귀 확인 방법 · 참고용

현재 남아 있는 테스트는 `OpenAiStoryServiceTest` 하나입니다. 네트워크를 타지 않는 단위 테스트 6개로, 프롬프트 조립 로직을 검증합니다.

```bash
./gradlew test --tests '*OpenAiStoryServiceTest'
```

- 첫 인사말 답장에 채점 지시문이 붙지 않는지 (신고되었던 버그의 재발 방지)
- 직전 턴에 퀴즈가 있을 때만 정답과 함께 채점 지시문이 붙는지
- 채점이 끝나면 지시문이 다시 사라지는지
- 퀴즈 5개 소진 시 추가 출제 금지 지시가 붙는지
- 사용자 입력에 `%` 가 섞여도 프롬프트 조립이 깨지지 않는지
- 오프닝 프롬프트에 `null` 이 들어가지 않는지

**프롬프트를 수정하면 이 테스트를 반드시 돌려주세요.** 특히 `String.format` 의 인자 개수는 컴파일러가 잡아주지 못하고 런타임에 터지는데, 이 테스트가 그 자리를 막아줍니다.

**실제 응답 품질**은 단위 테스트로 확인할 수 없습니다. 프롬프트를 크게 손본 뒤에는 실 API로 한 번 눈으로 확인하는 편이 좋습니다. 최초 검증에 사용했던 실 API 호출 테스트는 과금이 발생하고 일회성이라 삭제했으며, 당시 확인 결과는 [INTERACTIVE_STORY_PROMPTS.md](INTERACTIVE_STORY_PROMPTS.md) 4장의 실제 응답 예시로 남겨두었습니다.

---

## 8. 오답만 DB에 저장하는 방식이 맞는지 · 우선순위 높음 · 팀 논의 필요

**원래 합의된 방향.** 사용자가 인터랙티브 스토리를 진행하면서 **틀린 문제만** DB에 저장하기로 했습니다. 기존 학습 페이지의 오답 저장 방식(`wrong_answer` 테이블)을 참고하는 것이 전제였습니다.

**다시 생각해볼 필요가 있는 이유.** 기존 오답 저장 구조를 실제로 들여다보면, 인터랙티브 스토리 퀴즈는 그 구조가 요구하는 전제를 충족하지 못합니다. 구현에 들어가기 전에 팀에서 한 번 더 확인하는 게 좋겠습니다.

### 기존 `wrong_answer` 구조가 요구하는 것

| 컬럼 | 제약 | 출처 (기본 학습 페이지) | 인터랙티브 스토리에 있는가 |
|---|---|---|---|
| `quiz_content_id` | NOT NULL, `(user_id, quiz_content_id)` 유니크 | `quiz_content` 테이블의 PK | **없음** — LLM이 실시간 생성, DB에 행이 없음 |
| `content_id` | NOT NULL | `content` 테이블의 에피소드 ID | **없음** — 사용자가 자유 입력한 상황 |
| `story_name` | NOT NULL | `content.title` | 상황 설명 텍스트로 대체는 가능 |
| `level` | NOT NULL | `quiz_detail.difficulty` | **없음** — 난이도 개념이 없음 |

핵심은 `quiz_content_id`입니다. 기존 오답 노트는 **DB에 미리 존재하는 문제를 가리키는 참조**로 동작합니다. 인터랙티브 스토리 퀴즈는 매 세션 새로 생성되는 일회성 문제라 가리킬 대상이 없습니다.

### 파급 효과

`WrongAnswerRepository`의 **모든 조회 쿼리가 `QuizContent`와 JOIN** 합니다. 대시보드 오답 개수, 오답 에피소드 목록(`/incorrect`), 다시 풀기 목록(`/incorrect/retry`)이 전부 해당됩니다. 따라서 `quiz_content` 행 없이 저장된 오답은 **어느 화면에도 나타나지 않습니다.** 저장은 되지만 아무도 못 보는 데이터가 됩니다.

또한 오답 다시 풀기 흐름(`/incorrect/retry` → `/incorrect/result`)은 `contentId`로 묶어서 조회하고 `quizContentId`로 삭제합니다. 인터랙티브 스토리 오답을 여기에 태우려면 이 흐름 전체가 두 종류의 문제를 구분해 다룰 수 있어야 합니다.

### 선택지

**(A) 생성된 퀴즈를 `quiz_content`에 저장하고 기존 구조를 그대로 사용**
기존 오답 노트 화면과 다시 풀기 기능을 그대로 재사용할 수 있습니다. 대신 `content` → `quiz_detail` → `quiz_content` 행을 함께 만들어야 하고, 사용자마다 매번 새 문제가 생기므로 이 테이블들이 빠르게 커집니다. 기존 테이블은 큐레이션된 학습 콘텐츠를 담는 곳인데 사용자 생성 데이터가 섞이는 점도 고려해야 합니다.

**(B) 인터랙티브 스토리 전용 오답 테이블을 분리**
`story_wrong_answer` 같은 별도 테이블에 문제·정답·해설을 **값 그대로** 저장합니다. 기존 콘텐츠 테이블을 오염시키지 않고 스키마도 단순합니다. 대신 오답 노트 화면과 다시 풀기 기능을 스토리용으로 따로 만들거나, 두 소스를 합쳐 보여주도록 수정해야 합니다. 참고로 봇 컴피티션도 `competition_wrong_answer`로 테이블을 분리해 둔 선례가 있습니다.

**(C) 저장하지 않음**
인터랙티브 스토리를 오답 복습이 아니라 회화 연습 중심 기능으로 규정하는 선택입니다. 작업량이 없지만 학습 데이터가 남지 않습니다.

### 함께 정할 것

- 오답만 저장할지, 정답까지 저장할지. 현재는 오답만 저장하는 방향이지만, 학습 이력이나 통계를 나중에 붙일 계획이라면 기본 학습 페이지처럼 `user_study_log`에 전부 남기는 편이 유리할 수 있습니다.
- 저장 시점. 인터랙티브 스토리에는 기본 학습의 `/questionResult`에 해당하는 **결과 저장 엔드포인트가 아직 없습니다.** 매 턴 채점 직후 저장할지, 세션 종료 시 한 번에 저장할지 정해야 합니다. 세션이 메모리에만 있으므로(2번 항목) 종료 시 일괄 저장을 택하면 중간에 서버가 재시작될 때 전부 유실됩니다.
- 채점 신뢰성 선행 여부. 1번 항목(서버 측 결정론적 채점)이 해결되지 않은 상태로 오답을 저장하면, 모델이 잘못 판정한 결과가 그대로 DB에 남습니다. **1번을 먼저 처리하고 저장을 붙이는 순서**를 권합니다.

---

## 9. 기본 학습 페이지와 퀴즈 양식이 호환되지 않음 · 우선순위 높음

기본 학습 페이지(`POST /chat` → `POST /questionResult`)와 인터랙티브 스토리(`POST /api/v1/story/chat`)가 내려주는 퀴즈 JSON을 비교한 결과, **두 API의 양식이 서로 호환되지 않습니다.** 봇 컴피티션은 비교 대상에서 제외했습니다.

같은 "퀴즈"를 다루는 두 엔드포인트인데 필드 이름 규칙, 값 어휘, 식별자 유무, 채점 흐름이 모두 다릅니다. 클라이언트 작업이 시작되기 전에 양식을 맞춰두는 편이 비용이 적습니다.

### 9-1. `quiz_type` 값 어휘가 서로 다름 — 가장 시급

DB에 실제로 들어있는 값(`sql/` 덤프 기준)과 인터랙티브 스토리가 출제하는 값이 어긋납니다.

| quiz_type | 기본 학습 (DB 실제 데이터) | 인터랙티브 스토리 |
|---|---|---|
| `subjective` | 285건 | 출제함 |
| `multiple_choice` | 150건 | 출제함 |
| `fill_in_blank` | 15건 | **출제 안 함** |
| `word_arrange` | **0건 — 기존에 없던 유형** | 출제함 |

`word_arrange` 는 기존 데이터에 한 건도 없는 새 유형입니다. 게다가 이 유형만 `options` 대신 `tiles` 배열을 쓰기 때문에, 두 API를 하나의 퀴즈 처리 로직으로 다루려면 별도 분기가 반드시 필요합니다. 스토리 프롬프트가 "한 세션에 최소 1~2개는 `word_arrange` 또는 `subjective`"를 지시하므로 드물게 나오는 것도 아닙니다.

반대로 `fill_in_blank` 은 기본 학습에만 있습니다. 스토리 프롬프트의 Format A(빈칸 채우기)는 `quiz_type` 을 `multiple_choice` 로 출제하도록 되어 있어, 같은 형태의 문제가 서로 다른 유형명으로 나갑니다.

**정할 것:** `word_arrange` 를 유지할지 여부. 빼려면 프롬프트에서 Format D와 "최소 1~2개" 지시를 제거하면 되고, 유지하려면 `tiles` 를 포함한 유형 목록을 두 API가 공유하는 것으로 확정해야 합니다.

### 9-2. 필드 이름 규칙이 반대

| | 기본 학습 (`ChatResponseDto`) | 인터랙티브 스토리 (`StoryChatResponse`) |
|---|---|---|
| 규칙 | camelCase | snake_case (`@JsonProperty` 로 지정) |
| 예시 | `quizId`, `quizType`, `correctAnswer` | `quiz_type`, `correct_answer`, `ai_message`, `is_quiz` |

같은 의미의 필드가 다른 이름으로 나갑니다. 스토리 응답을 camelCase로 통일하면 클라이언트가 하나의 퀴즈 모델을 공유할 수 있습니다.

### 9-3. `options` 의 타입이 다름

| | 기본 학습 | 인터랙티브 스토리 |
|---|---|---|
| 타입 | `String` — DB JSON 컬럼 원문을 그대로 전달 | 실제 JSON 배열 |

`ChatResponseDto.QuizDto.options` 는 `String` 이라 받는 쪽이 한 번 더 파싱해야 합니다. 스토리는 배열을 그대로 내려줍니다. 같은 필드명인데 받는 쪽 처리가 달라지므로, 어느 한쪽으로 맞추는 게 좋습니다. 배열 쪽이 자연스럽습니다.

### 9-4. 퀴즈 식별자가 없음

기본 학습은 `quizId` / `quizContentId` 라는 DB PK를 기준으로 채점 결과 저장, 오답 저장, 다시 풀기가 모두 동작합니다. 인터랙티브 스토리 퀴즈는 실시간 생성이라 **ID가 없습니다.** `quiz_number` 는 세션 안에서의 1~5 순번일 뿐 전역 식별자가 아닙니다. 8번 항목(오답 저장)의 어려움이 여기서 파생됩니다.

### 9-5. 채점 흐름이 정반대

| | 기본 학습 | 인터랙티브 스토리 |
|---|---|---|
| 채점 주체 | 클라이언트 | LLM (서버 경유) |
| 결과 이동 방향 | 클라이언트 → 서버 (`correct` 불리언을 받음) | 서버 → 클라이언트 (`answer_result` 를 내려줌) |
| 전달 방식 | 배치 — 다 풀고 한 번에 | 스트리밍 — 턴마다 1개 |
| 결과 저장 API | `POST /questionResult` | **없음** |

`/questionResult` 는 클라이언트가 채점한 결과를 받는 구조입니다. 스토리는 서버가 채점 결과를 내려주는 구조라 방향이 반대이고, 퀴즈 ID도 없어서(9-4) 이 엔드포인트를 그대로 재사용할 수 없습니다.

### 권장 순서

1. `word_arrange` 유지 여부 결정 (9-1)
2. 스토리 응답의 필드 이름과 `options` 타입을 기본 학습 쪽과 통일 (9-2, 9-3)
3. 그다음 8번 항목(오답 저장 방식)과 결과 저장 엔드포인트 설계

---

## 참고 — 검수 범위와 한계

- 검증한 것: 컴파일, 단위 테스트 6개(프롬프트 조립 로직), 실제 OpenAI API 호출 4건(첫 인사말 답장·퀴즈 정답·퀴즈 오답·오프닝 생성 — 모두 의도대로 동작 확인, 검증에 쓴 테스트는 일회성이라 이후 삭제)
- 검증하지 못한 것: 앱을 실제로 기동한 end-to-end 확인. DB가 필요해 수행하지 못했습니다. 세션 소유권 검증, TTL 정리, 실패 시 롤백, 타임아웃 설정은 코드 검토로만 확인했으며 실제 요청으로 시험하지 않았습니다.
