# 큐링 전체 기능 테스트 케이스 (인증 · 마이페이지 · 콘텐츠 · 기본 학습 · 오답노트 · 대시보드)

> 별도 문서로 관리되는 기능:
> - 봇 컴피티션 → `qring_bot_competition_test_cases.md`
> - 인터랙티브 스토리 → `INTERACTIVE_STORY_TEST_CASES.md`
>
> 확정 스펙 기준
> - JWT: 액세스 1시간 / 리프레시 14일, 인증 실패는 현재 본문 없는 403 (401+JSON 개선안은 팀 합의 대기)
> - 이메일 인증 코드: 메모리 보관, 1회용(성공/만료 시 제거), 일회용(disposable) 이메일 도메인 차단
> - 기본 학습 채점: 클라이언트 채점 → `/questionResult` 일괄 제출, 점수 = 난이도·시도횟수·힌트 기반, 획득 점수만큼 포인트(토리) 지급
> - 오답노트: 정답 처리 시 목록에서 삭제, 다시 풀기는 맞든 틀리든 목록에서 제거 (정답 +3 / 오답 +1)
> - 언어 스코프: 콘텐츠 완료 여부·오답·대시보드 통계는 사용자의 현재 학습 언어 기준
> - 연속 학습일: 최근 학습일이 오늘 또는 어제일 때만 카운트, 그 전에 끊겼으면 0

---

## 1. 회원가입 — `POST /api/v1/auth/signup`

| # | 테스트 케이스 | 입력 | 기대 결과 |
|---|---|---|---|
| 1 | 정상 가입 | 유효한 이메일·비밀번호·닉네임 | 가입 성공, 인증 코드 메일 발송 (dev-mode 시 로그 출력) |
| 2 | 중복 이메일 (인증 완료 계정) | 인증까지 마친 이메일 | 400 `EMAIL_ALREADY_EXISTS` |
| 3 | 중복 닉네임 | 이미 사용 중인 닉네임 | 400 `NICKNAME_ALREADY_EXISTS` |
| 4 | 일회용 이메일 차단 | tempmail.com 등 차단 도메인 | 400 `DISPOSABLE_EMAIL_NOT_ALLOWED` |
| 5 | 일회용 도메인 대소문자 우회 | TempMail.COM | 동일하게 차단되는지 |
| 6 | 이메일 형식 오류 | "abc", "a@b" | 400 검증 오류 |
| 7 | 필수 필드 누락 | 이메일/비밀번호/닉네임 각각 누락 | 400 |
| 8 | 비밀번호 정책 | 너무 짧은/공백 비밀번호 | 정책 존재 여부 확인 (없다면 정책 확정 필요) |
| 9 | 가입 직후 미인증 상태 | 가입만 하고 로그인 시도 | `EMAIL_NOT_VERIFIED` 로 로그인 거부 |
| 10 | 메일 발송 실패 시 처리 | SMTP 오류 시뮬레이션 | `EMAIL_SEND_FAILED`, 가입 레코드 상태 확인 (재발송으로 복구 가능한지) |

---

## 2. 이메일 인증 — `POST /verify-email`, `POST /resend-code`

| # | 테스트 케이스 | 입력 | 기대 결과 |
|---|---|---|---|
| 11 | 정상 인증 | 발송된 코드 정확히 입력 | 인증 완료, 이후 로그인 가능 |
| 12 | 잘못된 코드 | 다른 숫자 입력 | 400 `CODE_MISMATCH`, 코드는 유지되어 재시도 가능 |
| 13 | 만료된 코드 | 발송 후 10분(TTL) 경과 후 입력 | 400 `CODE_EXPIRED` |
| 14 | 코드 없음 | 발송 이력 없는 이메일로 검증 | 400 `CODE_NOT_FOUND_OR_EXPIRED` |
| 15 | 코드 1회용 검증 | 성공한 코드로 재검증 | 400 (성공 시 제거되어 재사용 불가) |
| 16 | 재발송 정상 | 미인증 계정으로 resend | 새 코드 발송, 이전 코드는 맵 덮어쓰기로 무효화 (새 코드만 유효) |
| 17 | 이미 인증된 계정 재발송 | 인증 완료 후 resend | 400 `EMAIL_ALREADY_VERIFIED` |
| 18 | 재발송 연타 | 짧은 간격 여러 번 요청 | 발송 제한(rate limit) 존재 여부 확인 (없다면 메일 폭탄 위험 — 정책 확정 필요) |
| 19 | 서버 재시작 후 코드 유실 | 코드 발송 → 서버 재시작 → 검증 | 메모리 보관이므로 `CODE_NOT_FOUND_OR_EXPIRED` — 재발송 안내가 자연스러운지 |

---

## 3. 로그인 / 토큰 — `POST /login`, `POST /logout`, `POST /refresh`, `GET /me`

| # | 테스트 케이스 | 입력 | 기대 결과 |
|---|---|---|---|
| 20 | 정상 로그인 | 인증 완료 계정 | 액세스+리프레시 토큰 발급 |
| 21 | 비밀번호 오류 | 틀린 비밀번호 | 400 `INVALID_CREDENTIALS` (계정 존재 여부 노출 안 되는지) |
| 22 | 없는 이메일 | 미가입 이메일 | 400 `INVALID_CREDENTIALS` (동일 메시지) |
| 23 | 미인증 계정 | 인증 전 로그인 | 400 `EMAIL_NOT_VERIFIED` |
| 24 | 소셜 계정으로 일반 로그인 | 구글 가입 계정 + 비밀번호 로그인 | 400 `SOCIAL_LOGIN_ACCOUNT` |
| 25 | 액세스 토큰 정상 사용 | 발급 토큰으로 보호 API 호출 | 200 |
| 26 | 액세스 토큰 만료 | 1시간 경과 토큰 | 403 (현재 스펙 — 스토리 등 장시간 기능 사용 중 만료 시 `/refresh` 흐름 동작 확인) |
| 27 | 토큰 서명 위조 | secret 다른 토큰 | 403 |
| 28 | 리프레시 정상 | 유효한 refresh | 새 액세스 토큰 발급 |
| 29 | 리프레시 토큰 무효 | 위조/만료 refresh | 400 `INVALID_REFRESH_TOKEN` |
| 30 | 로그아웃 후 리프레시 | logout → 기존 refresh 사용 | **[코드 확정] 현재 서버 무효화 없음(무상태)** — 로그아웃 후에도 리프레시 성공함. 이대로 둘지 보안 정책 결정 필요 |
| 31 | /me 정상 | 유효 토큰 | 본인 프로필 반환 |
| 32 | /me 토큰 없음 | 헤더 없음 | 403 |

---

## 4. 소셜 로그인 — `POST /oauth/google · kakao · line`, `GET /oauth/{provider}`

| # | 테스트 케이스 | 입력 | 기대 결과 |
|---|---|---|---|
| 33 | 구글 최초 로그인 (신규) | 유효한 구글 인증 | 계정 생성 + 토큰 발급, 닉네임 처리 흐름 진입 |
| 34 | 구글 재로그인 (기존) | 기존 소셜 계정 | 신규 생성 없이 로그인 |
| 35 | 카카오 / 라인 동일 흐름 | 각 provider | provider 별 동일 동작 |
| 36 | 이메일 충돌: 타 방식 선가입 | 일반 가입 이메일로 구글 로그인 | 400 `EMAIL_ALREADY_USED_BY_LOCAL` (provider 명시) |
| 37 | 이메일 충돌: 타 소셜 선가입 | 구글 가입 이메일로 카카오 로그인 | 400 `EMAIL_ALREADY_USED_BY_GOOGLE` |
| 38 | 소셜 인증 실패 | 무효한 인가 코드/토큰 | 400 `SOCIAL_LOGIN_FAILED` |
| 39 | 소셜 닉네임 중복 | 소셜 가입 시 기존 닉네임과 충돌 | `NICKNAME_ALREADY_EXISTS` 처리 → 닉네임 재입력 흐름 |
| 40 | 리다이렉트 페이지 | GET /oauth/google 브라우저 접근 | HTML(UTF-8) 정상 렌더, 앱으로 파라미터 전달 |
| 41 | 소셜 계정은 이메일 인증 불요 | 소셜 가입 직후 | verify 없이 바로 서비스 사용 가능 |

---

## 5. 중복 확인 — `GET /check-email`, `GET /check-nickname`

| # | 테스트 케이스 | 입력 | 기대 결과 |
|---|---|---|---|
| 42 | 사용 가능 이메일 | 미가입 이메일 | 사용 가능 응답 |
| 43 | 중복 이메일 | 가입된 이메일 | 중복 응답 |
| 44 | 사용 가능/중복 닉네임 | 각각 | 정확한 가용성 응답 |
| 45 | 닉네임 앞뒤 공백/대소문자 | " Kanta " vs "kanta" | 정규화 기준 일관성 확인 (가입 시 검사와 동일 기준인지) |
| 46 | 인증 없이 호출 가능 여부 | 토큰 없이 호출 | permitAll 이므로 200 |

---

## 6. 마이페이지 — `POST /mypage`, `/mypage/setting`, `/update`, `/mypage/learning`, `/langcheck`, `/switch`

| # | 테스트 케이스 | 입력 | 기대 결과 |
|---|---|---|---|
| 47 | 마이페이지 조회 | 유효 토큰 | 프로필·포인트 등 본인 정보 반환 |
| 48 | 설정 조회 | /mypage/setting | 현재 설정값 반환 |
| 49 | 닉네임 변경 정상 | 새 닉네임 | 변경 반영, 이후 /me 에서 확인 |
| 50 | 닉네임 변경 중복 | 타인이 쓰는 닉네임 | **[수정 완료]** 400 `NICKNAME_ALREADY_EXISTS`, 기존 값 유지 (자기 자신과 같은 값은 통과) |
| 51 | 자기 자신 닉네임으로 변경 | 현재와 동일 닉네임 | 성공 (자기 자신은 중복 아님) 확인 |
| 52 | 비밀번호 변경 | password 포함 update | 변경 후 새 비밀번호로만 로그인 가능 |
| 53 | 소셜 계정 비밀번호 변경 시도 | 소셜 계정 + password | 거부 또는 무시 정책 확인 |
| 54 | 푸시 설정 토글 | pushEnabled true/false | 저장 및 재조회 일치 |
| 55 | 부분 업데이트 | nickname 만 보내고 password 생략 | 생략 필드는 기존 값 유지 (null 로 덮어쓰지 않음) |
| 56 | 학습 설정 변경 | /mypage/learning | 난이도/학습 목표 등 반영 |
| 57 | 언어 보유 확인 | /langcheck?lang=en | 해당 언어 학습 이력 유무 boolean |
| 58 | 언어 전환 | /switch | user.language 변경 → 이후 콘텐츠/오답/대시보드가 새 언어 스코프로 조회 |
| 59 | 미지원 언어 전환 | 존재하지 않는 언어 코드 | 오류 또는 거부 정책 확인 |
| 60 | 언어 전환 후 진행도 격리 | 한국어 완료 후 영어로 전환 | 영어 쪽 진행도/오답은 독립적으로 0에서 시작 |

---

## 7. 콘텐츠 목록 / 해금 — `POST /contentList`, `POST /content/{contentId}/unlock`

### 7-1. 목록

| # | 테스트 케이스 | 조건 | 기대 결과 |
|---|---|---|---|
| 61 | 목록 정상 조회 | 유효 토큰 | contentId·categoryName·thumbnailUrl·title·quizCount·isCompleted·status·requiredPoints |
| 62 | 카테고리 값 정상 | 여러 카테고리 콘텐츠 | 각 항목의 categoryName 으로 프론트 필터 가능 |
| 63 | isCompleted 언어 스코프 (회귀) | 한국어 완료·영어 미완료 유저가 영어로 조회 | 영어 콘텐츠 isCompleted=false (언어 구분 없이 true 나오던 버그) |
| 64 | 완료 처리 반영 | /questionResult 제출 후 재조회 | 해당 콘텐츠 isCompleted=true |
| 65 | 해금 상태 반영 | 해금 전/후 조회 | status(LOCKED/UNLOCKED)와 requiredPoints 정확 |

### 7-2. 해금

| # | 테스트 케이스 | 조건 | 기대 결과 |
|---|---|---|---|
| 66 | 유료 콘텐츠 정상 해금 | requiredPoints=N, 잔액≥N | -N 차감, 영구 해금 레코드 생성, balance_after 정확 |
| 67 | 무료 콘텐츠 해금 | requiredPoints=0 또는 null | 차감 없이 UNLOCKED 응답 |
| 68 | 잔액 = 필요 포인트 정확히 | 잔액 N, 필요 N | 성공, 잔액 0 |
| 69 | 잔액 부족 | 잔액 N-1 | 400 "포인트가 부족합니다", 차감 없음 |
| 70 | 이미 해금된 콘텐츠 재해금 | 해금 후 재요청 | **[코드 확정] 중복 체크 있음** — 무과금 UNLOCKED 재응답 (정상 동작 기대) |
| 71 | 없는 contentId | 존재하지 않는 ID | 400/404 |
| 72 | 동시 해금 요청 | 같은 콘텐츠 동시 2회 | 차감 1회만 발생하는지 (race condition) |
| 73 | 해금 후 영구성 | 해금 → 로그아웃/재로그인 → 조회 | 해금 상태 유지 |

---

## 8. 기본 학습 조회 — `POST /chat?contentId=`

| # | 테스트 케이스 | 조건 | 기대 결과 |
|---|---|---|---|
| 74 | 정상 조회 | 유효 contentId | scripts(대사) + quizzes(문제) 배열 반환 |
| 75 | 언어·난이도 매칭 | 유저 language=en, level=2 | 해당 언어/난이도의 스크립트·문제만 반환 |
| 76 | 퀴즈-스크립트 연결 | 응답 구조 | quiz.scriptId 가 scripts 의 scriptId 와 연결되어 출현 위치 결정 가능 |
| 77 | 퀴즈 필드 완전성 | 각 quiz | quizId·quizType·question·options·correctAnswer·explanation·hint 존재 |
| 78 | options 문자열 파싱 | multiple_choice 문제 | options 가 JSON 문자열로 오는 현재 스펙 — 프론트 파싱 정상 (배열 통일 개선안은 별도) |
| 79 | quizType 값 검증 | 전체 문제 | multiple_choice/subjective/fill_in_blank 만 존재 |
| 80 | 없는 contentId | 임의 ID | 오류 또는 빈 응답 정책 확인 |
| 81 | 잠긴 콘텐츠 직접 조회 | 미해금 유료 콘텐츠 chat 호출 | **[수정 완료]** 400 "해금이 필요한 콘텐츠입니다" — 해금/무료 콘텐츠만 통과 (기존엔 검증 없이 정답까지 노출) |
| 82 | 학습 언어 미설정 유저 | language=null | NPE 없이 명확한 처리 |

---

## 9. 학습 결과 저장 — `POST /questionResult`

### 9-1. 점수·포인트

| # | 테스트 케이스 | 조건 | 기대 결과 |
|---|---|---|---|
| 83 | 전부 정답 제출 | 모든 문제 correct=true | totalScore = 문제별 계산 점수 합, correctCount 정확 |
| 84 | 난이도별 점수 차등 | 난이도 1/2/3 문제 각각 정답 | 난이도 높을수록 높은 점수 (score_table 기준) |
| 85 | 시도 횟수 반영 | attemptCount 1 vs 3 | 시도 많을수록 낮은 점수 |
| 86 | 힌트 사용 반영 | hintUsed=true | 감점 반영 |
| 87 | 오답의 기본 점수 | correct=false | 시도 4회 기준 최저 점수 부여 (0점 아님 — 정책 확인) |
| 88 | 포인트 지급 = 점수 | 제출 완료 후 잔액 | 계산된 totalScore 만큼 토리 증가 |
| 89 | 첫 스토리 완료 보너스 | 해당 언어 첫 완료 | +30 추가 지급 |
| 90 | 두 번째 스토리 완료 | 같은 언어 두 번째 완료 | +30 미지급 (첫 완료만) |
| 91 | 언어별 첫 완료 보너스 독립 | 한국어 첫 완료 후 영어 첫 완료 | 영어에서도 +30 지급되는지 (언어별 1회 정책 확인) |
| 92 | 동일 콘텐츠 재학습 제출 | 완료한 콘텐츠 재제출 | 포인트 재지급 여부·진도 중복 처리 정책 확인 (무한 파밍 가능하면 구멍) |

### 9-2. 기록·진도

| # | 테스트 케이스 | 조건 | 기대 결과 |
|---|---|---|---|
| 93 | user_study_log 기록 | 제출 후 DB | 문제별 응답·정오·언어 기록 |
| 94 | story_progress 완료 처리 | 제출 후 | (userId, contentId, language) 완료 레코드, completedAt 기록 |
| 95 | user_progress 100% | 제출 후 | progressRate=100 |
| 96 | 진도의 언어 스코프 | 영어로 완료 | 한국어 진도에 영향 없음 |

### 9-3. 오답 저장

| # | 테스트 케이스 | 조건 | 기대 결과 |
|---|---|---|---|
| 97 | 오답 저장 | correct=false 문제 | wrong_answer 에 유저 언어의 quiz_content 기준으로 저장 |
| 98 | 오답 중복 저장 방지 | 같은 문제 두 번 틀림 | (user, quiz_content) 유니크 — 1건만 유지 |
| 99 | 재학습에서 정답 시 오답 삭제 | 오답이던 문제 정답 제출 | wrong_answer 에서 삭제 |
| 100 | 전부 정답 시 오답 미생성 | 모두 correct | 오답 테이블 변화 없음 |
| 101 | 언어 미설정 유저의 오답 | language=null | 오답 저장 생략되어도 나머지(점수·로그) 정상 처리 |

### 9-4. 이상 입력

| # | 테스트 케이스 | 조건 | 기대 결과 |
|---|---|---|---|
| 102 | 존재하지 않는 quizId | 임의 ID 포함 제출 | 400 "퀴즈를 찾을 수 없습니다", 전체 롤백 (부분 지급 없음) |
| 103 | 빈 results 배열 | results=[] | 점수 0 정상 응답 또는 400 — 정책 확인 |
| 104 | 트랜잭션 원자성 | 저장 중 일부 단계 실패 | 포인트·로그·오답·진도 전체 롤백 |
| 105 | 타 콘텐츠 문제 섞어 제출 | 서로 다른 콘텐츠의 quizId 혼합 | content 결정 로직(첫 문제 기준)의 동작 확인 — 진도 오처리 여부 |

---

## 10. 오답노트 — `POST /incorrect`, `/incorrect/retry`, `/incorrect/result`

| # | 테스트 케이스 | 조건 | 기대 결과 |
|---|---|---|---|
| 106 | 오답 에피소드 목록 | 오답 보유 유저 | contentId+storyName 중복 없이(distinct) 반환 |
| 107 | 목록 언어 스코프 | 한국어/영어 오답 각각 보유, 현재 영어 | 영어 오답의 에피소드만 |
| 108 | 오답 0건 | 오답 없는 유저 | 빈 목록 |
| 109 | 다시 풀기 문제 조회 | contentId 지정 | 해당 에피소드 오답 문제들 — question·options·hint·correctAnswer·quizType |
| 110 | 다시 풀기: 정답 제출 | correct=true | 목록에서 삭제 + **+3pt** |
| 111 | 다시 풀기: 또 오답 | correct=false | **그래도 목록에서 삭제** + +1pt (재도전 1회 정책) |
| 112 | 혼합 제출 포인트 합산 | 정답 2 + 오답 3 | totalPoint = 2×3 + 3×1 = 9 |
| 113 | 다시 풀기 후 목록 재조회 | 전부 제출 후 /incorrect | 해당 에피소드 사라짐 |
| 114 | 이미 삭제된 오답 재제출 | 같은 quizContentId 재요청 | 중복 포인트 지급 여부 확인 (**delete 는 idempotent 라 포인트만 반복 지급될 수 있음 — 파밍 구멍 검증 필수**) |
| 115 | 타인 오답 제출 시도 | userB 의 quizContentId 로 제출 | 본인 목록 기준으로만 동작하는지 (타인 데이터 영향 없음) |
| 116 | 없는 contentId 로 retry | 임의 ID | 빈 목록 또는 오류 정책 확인 |

---

## 11. 대시보드 — `POST /api/v1/dash`

| # | 테스트 케이스 | 조건 | 기대 결과 |
|---|---|---|---|
| 117 | 정상 조회 필드 완전성 | 학습 이력 보유 유저 | name·consecutiveDays·progressRate·commentText·completedStoryCount·levelDesc·levelCode·weeklyStudy[7]·incorrectAlarm·isConsecutivePointReceived·currentPoints·incorrectQuizCount |
| 118 | 신규 유저 | 학습 이력 0 | 0 값들로 정상 응답 (NPE·500 없음) |
| 119 | 연속 학습일: 오늘 학습 | 오늘 포함 3일 연속 | consecutiveDays=3 |
| 120 | 연속 학습일: 어제까지 학습 | 어제까지 5일 연속, 오늘 미학습 | 5 유지 (어제 기준 허용) |
| 121 | 연속 학습일: 그저께가 마지막 | 이틀 공백 | 0 (끊김) |
| 122 | 주간 학습 배열 | 이번 주 월·수 학습 | weeklyStudy=[T,F,T,F,F,F,F] — 월요일 시작 7칸 |
| 123 | 주 경계 | 일요일 밤 학습 → 월요일 조회 | 지난주 학습이 새 주 배열에 포함되지 않음 |
| 124 | 오답 알람 발동 | 7일 이상 방치된 오답 존재 | "오답을 확인한 지 7일이 지났어요!" 문구 |
| 125 | 오답 알람 미발동 | 오답 없음 또는 7일 미만 | 빈 문자열 |
| 126 | 오답 알람 언어 스코프 | 타 언어 오답만 7일 경과 | 현재 언어 기준으로 알람 판단 |
| 127 | 주간 성취도·완료 수 정확성 | 이번 주 평일 2일 + 토요일 학습 | **[코드 확정]** progressRate 는 콘텐츠 평균이 아니라 **이번 주 학습률**: 평일 +10%/주말 +25% (예시 = 45), 매주 월요일 리셋. completedStoryCount 는 언어 스코프 실측 일치 |
| 128 | 성취 코멘트 매칭 | 진도율 구간별 | achievement_comment 의 구간 문구 반환 |
| 129 | 연속 학습 보상 (15일 주기) | 15·30·45일 연속 달성 후 대시보드 조회 | **[코드 확정]** 15일 배수 도달 시 **대시보드 조회 시점에** +30 지급, isConsecutivePointReceived=true. 같은 배수 재조회 시 중복 지급 없음 (streakDays 가드) |
| 130 | 오답 개수 | 오답 N건 | incorrectQuizCount=N (언어 스코프) |

---

## 12. 공통 — 보안 · 오류 형식 · 인프라

| # | 테스트 케이스 | 조건 | 기대 결과 |
|---|---|---|---|
| 131 | 보호 API 무토큰 일괄 확인 | 각 기능 대표 API 토큰 없이 호출 | 전부 403 (permitAll 목록: auth 계열·swagger·health·정적 리소스만) |
| 132 | 타 유저 데이터 접근 일괄 확인 | userA 토큰으로 userB 자원 접근 (마이페이지·오답·대시보드는 토큰 기준이라 구조적 안전 — ID 파라미터 받는 API 위주) | 소유권 검증 동작 |
| 133 | 오류 응답 형식 일관성 | 각 400/500 케이스 | `{success:false, code, message}` JSON 형식 |
| 134 | IllegalState 계열 500 반환 현황 | 포인트 부족(스토리)·종료 세션 등 | 현재 500 으로 나가는 목록 파악 (401/400 교정은 팀 합의 후 일괄) |
| 135 | Swagger 접근 | /swagger-ui, /v3/api-docs | 무인증 접근 가능, 전체 API 문서화 확인 |
| 136 | 헬스체크 | /actuator/health | 현재 actuator 미설치로 500 — 의존성 추가 여부 결정 (배포 헬스체크 필요 시) |
| 137 | 배포 직후 기동 지연 | docker compose up 직후 접속 | 앱 기동 ~70초 동안 502 — 그 후 정상 (헬스체크 연동 개선안 별도) |
| 138 | data.sql 미실행 (회귀) | 서버 재기동 후 | RDS 에 INSERT 스크립트 실행 흔적 없음 (sql.init.mode=never) |
| 139 | CORS | 프론트 도메인에서 호출 | 허용 (현재 전체 오리진 허용 — 운영 전 축소 검토) |
| 140 | 토큰 만료 UX 흐름 | 사용 중 1시간 경과 | 403 수신 → refresh → 원요청 재시도 흐름이 프론트에서 성립하는지 |

---

## 13. 2차 정밀 검토 추가 케이스 (서비스 구현 코드 확인 기반)

### 13-1. 시간대(타임존) — 서버가 UTC 로 도는 문제

앱 컨테이너에 TZ 설정이 없어 JVM 이 UTC 로 동작합니다 (`LocalDate.now()` 기준). DB 접속만 Asia/Seoul 입니다. 날짜 기반 로직 전부가 한국 시간과 9시간 어긋날 수 있습니다.

| # | 테스트 케이스 | 조건 | 기대 결과 |
|---|---|---|---|
| 141 | 연속 학습일 자정 경계 | KST 오전 8시(UTC 전날 23시)에 학습 → 대시보드 조회 | **학습이 "어제"로 집계되어 연속일이 어긋나는지** — 어긋나면 컨테이너 TZ=Asia/Seoul 설정 필요 |
| 142 | 주간 학습 배열 요일 경계 | KST 월요일 오전 학습 | weeklyStudy 에서 지난주 일요일로 표시되는지 |
| 143 | 오답 7일 알람 경계 | 정확히 7일째 | UTC/KST 어느 기준으로 발동하는지 일관성 |
| 144 | 스토리 세션 TTL 시각 | created/updated_at 기록 | 서버 UTC ↔ DB Seoul 혼용으로 TTL 3시간이 실제로는 다르게 동작하지 않는지 |

### 13-2. 인증 심화 (코드 확정 사실 기반)

| # | 테스트 케이스 | 조건 | 기대 결과 |
|---|---|---|---|
| 145 | 미인증 계정 재가입 덮어쓰기 | 가입만 하고 인증 안 한 이메일로 재가입 | **[코드 확정]** 기존 미인증 레코드 삭제 후 재가입 성공 (비밀번호·닉네임 새 값으로 교체) |
| 146 | 미인증 이메일의 check-email | 미인증 계정 이메일 조회 | available=true (재가입 허용 정책과 일관 — 의도된 동작인지 팀 확인) |
| 147 | check-email 에 일회용 도메인 | tempmail.com 주소 조회 | **available=true 로 나오지만 가입은 거부됨 — 검사 불일치.** check 단계에서도 거부할지 결정 |
| 148 | 리프레시 토큰 회전 없음 | refresh 를 여러 번 호출 | **[코드 확정]** 매번 새 페어 발급되지만 이전 리프레시도 만료 전까지 계속 유효 (토큰 증식). 탈취 시나리오 관점에서 rotation 도입 여부 결정 |
| 149 | 만료 직전 리프레시로 무한 연장 | 14일마다 refresh 반복 | 재로그인 없이 영구 세션 가능 — 의도된 정책인지 확인 |

### 13-3. 권한 — 관리자 API 보호 부재

| # | 테스트 케이스 | 조건 | 기대 결과 |
|---|---|---|---|
| 150 | 일반 유저의 문제 Import 호출 | 일반 계정 토큰으로 `/admin/competition/quiz/import` | **[코드 확정] 역할(role) 시스템이 없어 로그인만 하면 호출 가능** — 봇 컴피티션 문서 #69 의 "403 기대"는 현재 불만족. 아무나 출제 문제를 주입할 수 있으므로 role 도입 또는 별도 보호 필요 (심각) |
| 151 | admin 경로 인증 없이 호출 | 토큰 없음 | 403 (authenticated 는 걸려 있음) |

### 13-4. 해금 동시성·무결성 (코드 확정 사실 기반)

| # | 테스트 케이스 | 조건 | 기대 결과 |
|---|---|---|---|
| 152 | 동시 해금 이중 차감 | 같은 콘텐츠 동시 2회 (exists 체크와 저장 사이 race) | **check-then-act 라 이중 차감+중복 레코드 가능성 있음** — 재현되면 유니크 제약/원자적 처리 필요 |
| 153 | 서로 다른 콘텐츠 동시 해금로 음수 잔액 | 잔액 100, 각 100짜리 콘텐츠 2개 동시 해금 | **잔액 검사와 차감(`addPoints`)이 분리되어 음수 잔액 가능성 있음** — 스토리의 원자적 차감(tryDeductPoints)과 달리 무가드. 재현 여부 검증 |
| 154 | 무료 콘텐츠 해금 레코드 미생성 | requiredPoints=0 해금 후 목록 조회 | **[코드 확정]** 무료는 unlock 레코드를 저장하지 않고 응답만 UNLOCKED — 목록의 status 계산이 레코드 기반이면 계속 LOCKED 로 보일 수 있음. 정합성 확인 |
| 155 | 무료 해금 응답의 잔액 null | requiredPoints=0 첫 분기 | balance 가 null 로 내려감 — 프론트 널 처리 확인 |
| 156 | 포인트 이력 기록 | 유료 해금 후 user_asset_history | changeAmount·balanceAfter·CONTENT_UNLOCK·referenceId 정확 (다른 포인트 변동엔 이력이 없는 비대칭 — 이력 정책 통일 여부 확인) |

### 13-5. 마이페이지 심화 (코드 확정 사실 기반)

| # | 테스트 케이스 | 조건 | 기대 결과 |
|---|---|---|---|
| 157 | 소셜 계정에 비밀번호 설정 | 소셜 계정으로 /update 에 password 전달 | **[코드 확정] provider 확인 없이 인코딩·저장됨** — 저장돼도 로그인은 `SOCIAL_LOGIN_ACCOUNT` 로 계속 차단되는지 (차단된다면 무해하지만 혼란, 차단 안 되면 우회 경로) |
| 158 | 닉네임 길이·문자 제한 | 200자 닉네임, 이모지, 공백만 | 제한 없이 저장되는지 — 제한 정책 확정 필요 |
| 159 | 빈 문자열 password | password="" | 변경으로 취급 안 됨(기존 유지) — 명세대로인지 |
| 160 | 임의 문자열 언어 전환 | /switch 에 "klingon" | **[코드 확정] 검증 없이 저장됨** — 이후 콘텐츠/오답/대시보드가 빈 결과로 동작하는지, 지원 언어 화이트리스트 필요 여부 |
| 161 | 언어 전환 시 레벨 동기화 | 이력 있는 언어로 전환 | user_language_level 의 레벨로 levelCode 동기화 / 이력 없는 언어면 기존 levelCode 유지 (의도 확인) |

---

## 14. 3차 검토 — 봇 컴피티션 서비스 내부 · OAuth · 대시보드 (구현 코드 정밀 확인)

### 14-1. 봇 컴피티션 (별도 문서 보완 — 코드와 문서의 차이 발견)

| # | 테스트 케이스 | 조건 | 기대 결과 |
|---|---|---|---|
| 162 | 입장료 조작 차단 | entry_cost=0 / 음수 / 임의 값으로 /bot/level 호출 | **[수정 완료]** 서버가 레벨로 비용 확정 (하50/중70/상100), 요청 값은 무시하고 불일치 시 WARN 로그. 기존엔 프론트 값을 그대로 차감해 0/음수 조작으로 무료 입장·포인트 증식 가능했음 |
| 163 | 입장료 동시 요청 원자성 | 잔액 50으로 동시 매치 2회 시작 | **[수정 완료]** 원자적 차감으로 하나만 성공 |
| 164 | 스트릭 보너스 티어 코드-문서 불일치 | 14연속 / 21올백 달성 | **[코드 확정] 코드는 14연속 +25, 21올백 +35 — 컴피티션 문서 스펙(+20/+30)과 다름.** 어느 쪽이 확정인지 팀 결정 후 한쪽 수정 필요 |
| 165 | 결과 제출이 matchId 를 안 받음 | 진행 중 매치 2개 상태에서 /bot/result | **[코드 확정]** 첫 번째 활성 매치에 결과가 귀속됨 — 의도한 매치가 아닐 수 있음. 매치 1개 정책 강제 또는 matchId 파라미터 필요 |
| 166 | 진행 중 매치 있어도 새 매치 시작 | 미완료 매치 상태에서 /bot/level 재호출 | **[코드 확정]** 제한 없이 새 매치 생성됨 (입장료 중복 차감) — 봇 문서 #25 의 '정책 확인' 항목, 현재는 허용 |
| 167 | 21문제 개수 미검증 | answers 를 5개만 / 30개 제출 | **[코드 확정]** 개수 검증 없이 그대로 처리·보상 지급 — 부분 제출로 스트릭 조작 여지. 검증 추가 여부 결정 |
| 168 | 문제 풀 부족 시 21문제 미달 | 스토리/컴피티션 풀이 부족한 언어·레벨 | 부족한 만큼만 반환됨 (21개 보장 없음) — 데이터 임포트 상태 점검과 함께 확인 |
| 169 | botLevel 입력 계약 | "하"/"중"/"상" 외 값 | 400. **참고: 컴피티션 문서 #17~21 은 level=1/2/3 숫자 입력을 가정하나 실제 API 는 한글 문자열** — 문서 수정 필요 |
| 170 | 정답 여부 프론트 신뢰 (구조적) | 전부 정답으로 위조 제출 | 현재 설계상 만점 보상(+기본+35) 지급됨 — 기본 학습과 동일한 클라이언트 신뢰 구조. 부정 방지 필요성 팀 논의 |

### 14-2. 대시보드 심화 (코드 확정 사실 기반)

| # | 테스트 케이스 | 조건 | 기대 결과 |
|---|---|---|---|
| 171 | 주간 성취도 공식 | 월~금 5일 전부 + 주말 2일 학습 | progressRate = 5×10 + 2×25 = 100 |
| 172 | 15일 보상은 대시보드 조회가 트리거 | 15일 연속 달성 후 대시보드 미조회 | 조회 전까지 +30 미지급 (조회 시점 지급 구조 — 의도인지 확인) |
| 173 | 스트릭 끊김 후 재달성 보상 | 15일 달성·수령 → 끊김 → 다시 15일 달성 | **[코드 확정] streakDays(15)가 리셋되지 않아 두 번째 15일 보상이 지급되지 않음** (15 < 15 false) — 버그 의심, 정책 확인 |
| 174 | 신규 유저 asset 없음 | user_asset 미생성 유저 | currentPoints=0 으로 안전 처리 (NPE 없음) |

### 14-3. OAuth 심화 (코드 확정 사실 기반)

| # | 테스트 케이스 | 조건 | 기대 결과 |
|---|---|---|---|
| 175 | 이메일 미제공 소셜 가입 | 카카오 이메일 미동의 / LINE | `{socialId}@{provider}.qring.local` 합성 이메일로 가입됨 — 메일 발송 대상 아님 확인 |
| 176 | LINE 은 이메일 충돌 검사 없음 | LINE 가입 | 이메일이 항상 null 이라 기존 계정과의 이메일 충돌 검사가 적용되지 않음 (계정 통합 관점 확인) |
| 177 | 임시 닉네임 생성 규칙 | 소셜 신규 가입 | `user_` + socialId 뒷 8자리, 충돌 시 숫자 접미사 1~999 — 온보딩에서 확정 전까지 사용 |

---

**커버리지 요약**: 총 177개 케이스 — 회원가입 10 / 이메일 인증 9 / 로그인·토큰 13 / 소셜 9 / 중복확인 5 / 마이페이지 14 / 콘텐츠·해금 13 / 학습 조회 9 / 결과 저장 23 / 오답노트 11 / 대시보드 14 / 공통 10 / 2차 정밀 21 / 3차 정밀 16

**우선 실행 권장 (스모크 12선)**: #1 가입 → #11 인증 → #20 로그인 → #61 목록 → #66 해금 → #74 학습 조회 → #83 결과 제출 → #97 오답 저장 → #110 다시 풀기 → #117 대시보드 → #58 언어 전환 → #26 토큰 만료

**수정 완료된 이슈 (2026-09-10, 배포 후 해당 케이스로 검증할 것)**:
- ✅ **#162 봇 컴피티션 입장료 조작** — 서버가 레벨별 비용(50/70/100) 확정, 원자적 차감 (#163)
- ✅ **#81 미해금 콘텐츠 우회** — /chat 에 해금 검증 추가
- ✅ **#150 관리자 Import API 무방비** — `ADMIN_USER_IDS` 환경변수(쉼표 구분 userId) 등록자만 허용, 미설정 시 전원 차단. **.env 에 ADMIN_USER_IDS 추가 필요**
- ✅ **#50 마이페이지 닉네임 중복** — 가입과 동일한 중복 검사 추가
- ✅ **#153 해금 음수 잔액 가능성** — 원자적 차감으로 교체
- ✅ **#141 서버 UTC 타임존** — Dockerfile(-Duser.timezone) + docker-compose(TZ) 에 Asia/Seoul 고정. **재빌드 배포 후 적용**

**남은 이슈 (미수정, 심각도순)**:
1. **#114 오답 다시 풀기 포인트 파밍** — 삭제가 idempotent 라 같은 quizContentId 반복 제출로 포인트 무한 지급 가능 구조. 실행 검증 후 수정 필요.
2. **#92 완료 콘텐츠 재학습 포인트 재지급** — 재제출 차단 없음, 무한 파밍 가능성.
3. **#170 봇 컴피티션 결과 위조** — 클라이언트 채점 신뢰 구조 (기본 학습 #83 동일) — 설계 차원 논의 필요.
4. **#164 스트릭 티어 코드-문서 불일치** — +25/+35 vs +20/+30, 팀 확정 필요.
5. **#173 스트릭 재달성 보상 미지급** — streakDays 미리셋 엣지.
6. **#165·#166 봇 매치 다중 진행 문제** — matchId 미사용 + 중복 시작 허용.
7. **#30·#148 토큰 무효화·회전 없음** — 로그아웃이 서버에서 무의미, 리프레시 증식. 보안 정책 결정 필요.
